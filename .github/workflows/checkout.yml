name: checkout
on:
  workflow_dispatch:  

jobs:
  checkout:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          ref: dev
          token: ${{ secrets.GIT_TOKEN }}